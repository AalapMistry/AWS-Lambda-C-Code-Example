# CRUD Operations Lambda Function

This is a sample AWS Lambda function written in .NET Core for performing CRUD (Create, Read, Update, Delete) operations on a hypothetical "Item" entity.

## Function Overview

- `GetAllItems`: Retrieves all items.
- `GetItemById`: Retrieves an item by ID.
- `CreateItem`: Creates a new item.
- `UpdateItem`: Updates an existing item.
- `DeleteItem`: Deletes an item.

## Setup

1. **Prerequisites**:
   - .NET Core SDK
   - AWS CLI (optional, for deployment)

2. **Build**:
   - Run `dotnet build` to build the project.

3. **Run Locally**:
   - Use a tool like AWS SAM CLI to test the function locally.

4. **Deploy**:
   - Deploy the function using AWS Management Console, AWS CLI, or AWS Toolkit for Visual Studio.

5. **Configure API Gateway**:
   - Create or update an API Gateway to trigger the Lambda function for HTTP requests.

## Usage

- Use the API Gateway endpoint URLs to perform CRUD operations on the "Item" entity.

## Examples

- See `LambdaHandler.cs` for example code implementing the CRUD operations.

## License

This project is licensed under the MIT License - see the [LICENSE.md](LICENSE.md) file for details.
